# This is a global description of this script
# This represents a very simple testcase
@tag=test

testAlex = test2()
testJohn = test2("john")

validate(testAlex/isAlexForSure, "Should be alex")
validate(not(testAlex/isJohnForSure), "Should not be john")

validate(testJohn/isJohnForSure, "Should be john")
validate(not(testJohn/isAlexForSure), "Should not be alex")